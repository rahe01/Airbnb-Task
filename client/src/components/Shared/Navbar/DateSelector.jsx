import SearchbarHtml from './SearchbarHtml';

const DateSelector = () => {
    return (
        <div className="flex items-center space-x-4">
            <SearchbarHtml />
        </div>
    );
};

export default DateSelector;
